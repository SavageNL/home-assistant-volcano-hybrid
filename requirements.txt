colorlog==6.12.0
homeassistant==2026.7.4
pip>=26.1.2
ruff==0.16.0
mypy==2.3.0
pytest-homeassistant-custom-component==0.13.348
# Pinned to the versions the homeassistant bluetooth component ships with,
# see homeassistant/components/bluetooth/manifest.json
# (bleak-retry-connector 4.6.3 is out, but HA 2026.7.4 ships 4.6.1)
habluetooth==6.26.5
bleak==3.0.2
bleak-retry-connector==4.6.1
# Requirements of the bluetooth/usb components, needed to import the
# integration in tests
aiousbwatcher
serialx
aioesphomeapi
bluetooth-adapters
bluetooth-auto-recovery
bluetooth-data-tools
dbus-fast
